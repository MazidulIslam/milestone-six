//variable
var deposit = 0;
let deposit1 = 0;
const deposit2 = 0;

// condition 
if (condition) {
    // statement 
} else if(condition){
    // statement 
} else {
    // statement 
}

// switch case condition 

// array
const numbers = [33, 34, 44, 45, 55];
const numberCount = numbers.length;
numbers.pop();
numbers.push(111);
// check wheather 222 includes or not 
if (numbers.indexOf(222) != -1) {
    
}
if (numbers.includes(222)) {
    
}

// loop
// while, for 
for (const number of number) {
    
}
// function()
function fullName(first, second) {
    const name = first + ' ' + second;
}
const person = fullName('Rahim', 'Ali');

// object 
const bottle = {
    color: 'yellow',
    contain: 'water',
    price: '333'
}